import dashboard from './dashboard';
import services from './services';
import maintenance from './maintenance';
// import other from './other';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, services, maintenance]
};

export default menuItems;
